http:
  port: ${PORT:-3000}
admin:
  port: 9876
  host: localhost
apiEndpoints:
  products:
    host: "*"
    paths: ["/products", "/products/*", "/uploadImage"]
  authentication:
    host: "*"
    paths: ["/auth/*"]
  customers:
    host: "*"
    paths: ["/customers", "/customers/*"]
  toasters:
    host: "*"
    paths: ["/toasters", "/toasters/*"]
  deliveries:
    host: "*"
    paths: ["/deliveries"]
  returns:
    host: "*"
    paths: ["/returns"]
  billing-profile:
    host: "*"
    paths: ["/billing-profile"]
  history:
    host: "*"
    paths: ["/history"]
  payment:
    host: "*"
    paths: ["/payment"]
  return:
    host: "*"
    paths: ["/return"]
  subscription:
    host: "*"
    paths: ["/subscription"]
serviceEndpoints:
  productsService:
    url: "https://coffaine-products.herokuapp.com/api/v1"
  usersService:
    url: "https://coffaine-users.herokuapp.com/api/v1"
  deliveriesService:
    url: "https://coffaine-deliveries.herokuapp.com/api/v1"
  salesService:
    url: "https://coffaine-sales.herokuapp.com/api/v1"
policies:
  - basic-auth
  - cors
  - expression
  - key-auth
  - log
  - oauth2
  - proxy
  - rate-limit
pipelines:
  productsPipeline:
    apiEndpoints:
      - products
    policies:
      # Uncomment `key-auth:` when instructed to in the Getting Started guide.
      # - key-auth:
      - proxy:
          - action:
              serviceEndpoint: productsService
              changeOrigin: true
  usersPipeline:
    apiEndpoints:
      - authentication
      - customers
      - toasters
    policies:
      # Uncomment `key-auth:` when instructed to in the Getting Started guide.
      # - key-auth:
      - proxy:
          - action:
              serviceEndpoint: usersService
              changeOrigin: true
  deliveriesPipeline:
    apiEndpoints:
      - deliveries
      - returns
    policies:
      # Uncomment `key-auth:` when instructed to in the Getting Started guide.
      # - key-auth:
      - proxy:
          - action:
              serviceEndpoint: deliveriesService
              changeOrigin: true
  salesPipeline:
    apiEndpoints:
      - billing-profile
      - history
      - payment
      - return
      - subscription
    policies:
      # Uncomment `key-auth:` when instructed to in the Getting Started guide.
      # - key-auth:
      - proxy:
          - action:
              serviceEndpoint: salesService
              changeOrigin: true
